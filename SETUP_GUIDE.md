# 📱 WhatsApp Sender - Полная инструкция по установке и запуску

## 🔑 ВАЖНО: Система ключей доступа

Теперь приложение защищено системой ключей доступа. При первом запуске нужно ввести ключ активации.

**Тестовый ключ для быстрого старта**: `TESTKEYMASTERABC`

Ключ создается автоматически при первом запуске backend сервера.

Для приобретения ключей: [@lleeellll](https://t.me/lleeellll)

Подробнее о ключах см. [KEYS_GUIDE.md](KEYS_GUIDE.md)

---

## 🎯 Что было исправлено

### ✅ Отправка изображений и аудио
- **Множественные XPath селекторы** - теперь система пробует разные варианты селекторов для совместимости
- **Абсолютные пути к файлам** - все файлы преобразуются в абсолютные пути
- **Улучшенная обработка ошибок** - подробное логирование и обработка исключений
- **Правильные ожидания** - добавлены необходимые задержки для загрузки превью файлов
- **Проверка существования файлов** - перед отправкой проверяется наличие файлов

### ✅ Кнопка "Настроить профиль"
- **Полнофункциональный диалог** с информацией о профиле:
  - Имя профиля
  - Номер телефона
  - Дата создания
  - Последнее использование
  - Статистика (всего/успешно/ошибок)
- **Красивый дизайн** с иконками и цветовым оформлением
- **Форматирование дат** на русском языке

### ✅ Дополнительные улучшения
- **Автоматическое обновление времени последнего использования**
- **Улучшенная стабильность работы с Chrome** (отключение автоматизации, no-sandbox)
- **Очистка загруженных файлов** после отправки
- **Множественные селекторы** для элементов интерфейса WhatsApp

---

## 📋 Требования

### 1. Системные требования
- **Операционная система**: Windows 10/11, macOS, или Linux
- **Python**: версия 3.8 или выше
- **Node.js**: версия 16 или выше
- **Chrome браузер**: последняя версия
- **ChromeDriver**: совместимый с версией Chrome

### 2. Проверка установленных компонентов

```bash
# Проверка Python
python --version
# или
python3 --version

# Проверка Node.js
node --version

# Проверка npm
npm --version
```

---

## 🚀 Установка

### Шаг 0: Ключ доступа

При первом запуске backend сервера файл `access_keys.json` создается автоматически с тестовым ключом `TESTKEYMASTERABC`.

Вы можете сразу использовать этот ключ для активации приложения.

Для создания дополнительных ключей используйте:
```bash
cd python-backend
python key_manager.py
# Выберите опцию 1 и укажите количество ключей
```

### Шаг 1: Установка Python зависимостей

#### Windows:
```bash
# Перейдите в папку с проектом
cd путь/к/вашему/проекту

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите зависимости
cd python-backend
pip install -r requirements.txt
```

#### macOS/Linux:
```bash
# Перейдите в папку с проектом
cd путь/к/вашему/проекту

# Создайте виртуальное окружение (рекомендуется)
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
cd python-backend
pip install -r requirements.txt
```

### Шаг 2: Установка ChromeDriver

#### Автоматическая установка (рекомендуется):
```bash
pip install webdriver-manager
```

Затем обновите `whatsapp_sender.py`, добавив в начало файла:
```python
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.chrome.service import Service
```

И замените строку `driver = webdriver.Chrome(options=options)` на:
```python
service = Service(ChromeDriverManager().install())
driver = webdriver.Chrome(service=service, options=options)
```

#### Ручная установка:
1. Узнайте версию вашего Chrome: `chrome://version/`
2. Скачайте соответствующий ChromeDriver: https://chromedriver.chromium.org/downloads
3. Распакуйте и поместите в PATH или в папку проекта

### Шаг 3: Установка фронтенд зависимостей

```bash
# Вернитесь в корневую папку проекта
cd ..

# Установите npm зависимости
npm install
```

---

## 🎮 Запуск приложения

### ВАЖНО: Первый запуск

При первом запуске приложения вы увидите экран активации. Используйте ключ `TESTKEYMASTERABC` для быстрого доступа.

### Вариант 1: Локальный запуск (Рекомендуется для разработки)

Вам нужно запустить ДВА терминала одновременно:

#### Терминал 1 - Backend (Python API):
```bash
# Активируйте виртуальное окружение (если еще не активировано)
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Перейдите в папку backend
cd python-backend

# Запустите API сервер
python api_server.py
```

Вы должны увидеть:
```
Starting WhatsApp Sender API Server...
Server running on http://localhost:5000
...
```

#### Терминал 2 - Frontend (React):
```bash
# В корневой папке проекта запустите фронтенд
npm run dev
```

Вы должны увидеть:
```
  VITE v... ready in ... ms

  ➜  Local:   http://localhost:8080/
  ➜  Network: use --host to expose
```

Откройте браузер и перейдите на `http://localhost:8080`

---

### Вариант 2: Запуск с помощью скриптов

Создайте файл `start.bat` (Windows) или `start.sh` (macOS/Linux):

#### start.bat (Windows):
```batch
@echo off
echo Starting WhatsApp Sender...
echo.
echo Starting Backend...
start cmd /k "cd python-backend && venv\Scripts\activate && python api_server.py"
timeout /t 3
echo Starting Frontend...
start cmd /k "npm run dev"
echo.
echo Both servers are starting...
echo Backend: http://localhost:5000
echo Frontend: http://localhost:8080
```

#### start.sh (macOS/Linux):
```bash
#!/bin/bash
echo "Starting WhatsApp Sender..."
echo ""
echo "Starting Backend..."
cd python-backend
source ../venv/bin/activate
python api_server.py &
BACKEND_PID=$!
cd ..
sleep 3
echo "Starting Frontend..."
npm run dev &
FRONTEND_PID=$!
echo ""
echo "Both servers are running..."
echo "Backend: http://localhost:5000"
echo "Frontend: http://localhost:8080"
echo ""
echo "Press Ctrl+C to stop both servers"
wait
```

Запустите:
```bash
# Windows:
start.bat

# macOS/Linux:
chmod +x start.sh
./start.sh
```

---

## 📖 Использование

### 1. Создание профиля
1. Откройте приложение в браузере (`http://localhost:8080`)
2. Перейдите в раздел "Создать профиль"
3. Введите имя профиля
4. Нажмите "Создать профиль"
5. Откроется окно Chrome с WhatsApp Web
6. Отсканируйте QR-код с вашего телефона
7. Дождитесь успешной авторизации
8. Профиль будет сохранен автоматически

### 2. Просмотр информации о профиле
1. Перейдите в раздел "Профили"
2. Нажмите кнопку "Настроить" у нужного профиля
3. Откроется диалог с полной информацией:
   - Номер телефона
   - Дата создания
   - Последнее использование
   - Статистика отправленных сообщений

### 3. Отправка одиночного сообщения
1. Перейдите в раздел "Отправить сообщение"
2. Выберите профиль
3. Введите номер телефона (формат: +79991234567)
4. Введите текст сообщения
5. (Опционально) Добавьте изображение
6. (Опционально) Добавьте аудио файл
7. Нажмите "Отправить"
8. Дождитесь завершения отправки

### 4. Массовая рассылка
1. Перейдите в раздел "Массовая рассылка"
2. Добавьте список номеров (по одному на строку)
3. Настройте профили и сообщения для каждого
4. (Опционально) Добавьте медиа файлы для каждого профиля
5. Настройте задержку между сообщениями
6. Нажмите "Начать рассылку"

---

## 🔧 Решение проблем

### Проблема: "ChromeDriver не найден"
**Решение:**
```bash
pip install webdriver-manager
```
И обновите код как описано в разделе установки ChromeDriver.

### Проблема: "Не удается подключиться к серверу"
**Решение:**
- Убедитесь, что backend запущен (`python api_server.py`)
- Проверьте, что порт 5000 не занят другим приложением
- Попробуйте перезапустить backend

### Проблема: "Изображение/аудио не отправляется"
**Решение:**
- Убедитесь, что файлы существуют и доступны
- Проверьте формат файлов (изображения: jpg, png, gif; аудио: mp3, wav, ogg)
- Увеличьте задержки в коде (если WhatsApp Web загружается медленно)
- Проверьте логи в терминале backend для деталей ошибки

### Проблема: "Не открывается WhatsApp Web"
**Решение:**
- Обновите Chrome до последней версии
- Обновите ChromeDriver
- Проверьте подключение к интернету
- Попробуйте открыть https://web.whatsapp.com/ вручную в браузере

### Проблема: "Профиль не сохраняется"
**Решение:**
- Проверьте права на запись в папку проекта
- Убедитесь, что папка `profiles` создается автоматически
- Дождитесь полной авторизации перед закрытием окна Chrome

---

## 📁 Структура проекта

```
chat-blaster-kit/
├── python-backend/           # Backend сервер
│   ├── api_server.py        # Flask API
│   ├── whatsapp_sender.py   # Логика отправки
│   └── requirements.txt     # Python зависимости
├── src/                      # Frontend приложение
│   ├── pages/               # Страницы
│   │   ├── Profiles.tsx    # Страница профилей
│   │   ├── Send.tsx        # Отправка сообщения
│   │   └── MassSend.tsx    # Массовая рассылка
│   ├── lib/                 # Библиотеки
│   │   └── api.ts          # API клиент
│   └── components/          # Компоненты
├── profiles/                 # Сохраненные профили WhatsApp
├── logs/                     # Логи отправленных сообщений
├── uploads/                  # Временные файлы (создается автоматически)
└── package.json             # Frontend зависимости
```

---

## 🎨 Особенности интерфейса

- **Современный дизайн** с использованием Tailwind CSS и shadcn/ui
- **Адаптивная верстка** - работает на всех устройствах
- **Темная/светлая тема** - поддержка разных цветовых схем
- **Анимации и переходы** - плавные интерактивные элементы
- **Уведомления** - тосты для всех операций
- **Диалоги** - модальные окна для дополнительной информации

---

## 🛡️ Безопасность

1. **Не делитесь профилями** - каждый профиль содержит данные авторизации
2. **Используйте разные профили** для разных целей
3. **Делайте задержки** между сообщениями (минимум 20-30 секунд)
4. **Не отправляйте спам** - соблюдайте правила WhatsApp
5. **Регулярно обновляйте** - следите за обновлениями проекта

---

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи в терминале backend
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены
4. Проверьте, что порты 5000 и 8080 свободны

---

## 🎉 Готово!

Теперь у вас полностью рабочая система для рассылки сообщений в WhatsApp!

**Важно**: Используйте приложение ответственно и не нарушайте правила WhatsApp.
