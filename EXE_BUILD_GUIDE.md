# Подробный гайд по созданию EXE-файла

Этот гайд объясняет как собрать весь проект в один исполняемый EXE-файл с помощью PyInstaller.

## Что будет в результате?

Один EXE-файл, который:
- Содержит встроенный веб-интерфейс (фронтенд)
- Запускает локальный сервер
- Автоматически открывает браузер с приложением
- Работает без установки Python, Node.js или других зависимостей

## Требования

- Windows 10/11 (для создания Windows EXE)
- Python 3.8 или выше
- Node.js 18+ (для сборки фронтенда)
- PyInstaller

## Шаг 1: Подготовка фронтенда

### 1.1 Соберите фронтенд в статические файлы

Откройте терминал в корневой папке проекта:

```cmd
npm install
npm run build
```

Это создаст папку `dist/` со всеми статическими файлами интерфейса.

### 1.2 Скопируйте dist в папку бэкенда

```cmd
# Windows
xcopy /E /I dist python-backend\dist

# Linux/macOS
cp -r dist python-backend/dist
```

## Шаг 2: Модификация бэкенда для standalone режима

### 2.1 Создайте файл `main.py` в папке `python-backend`

```python
# python-backend/main.py
import os
import sys
import webbrowser
import threading
from pathlib import Path
from api_server import app

def get_resource_path(relative_path):
    """Получить абсолютный путь к ресурсу (работает для dev и для PyInstaller)"""
    try:
        # PyInstaller создает временную папку и хранит путь в _MEIPASS
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")
    
    return os.path.join(base_path, relative_path)

def open_browser():
    """Открыть браузер через 1.5 секунды после запуска сервера"""
    import time
    time.sleep(1.5)
    webbrowser.open('http://localhost:5000')

if __name__ == '__main__':
    # Настроить путь к статическим файлам
    dist_folder = get_resource_path('dist')
    
    # Настроить Flask для обслуживания статических файлов
    from flask import send_from_directory
    
    @app.route('/', defaults={'path': ''})
    @app.route('/<path:path>')
    def serve_frontend(path):
        if path and Path(dist_folder, path).exists():
            return send_from_directory(dist_folder, path)
        return send_from_directory(dist_folder, 'index.html')
    
    # Открыть браузер в отдельном потоке
    threading.Thread(target=open_browser, daemon=True).start()
    
    # Запустить сервер
    print("🚀 WhatsApp Sender запущен!")
    print("📱 Откройте браузер: http://localhost:5000")
    print("⏹️  Для остановки нажмите Ctrl+C")
    
    app.run(host='0.0.0.0', port=5000, debug=False)
```

### 2.2 Обновите `api_server.py`

В начале файла добавьте:

```python
from flask import Flask, send_from_directory
from flask_cors import CORS

app = Flask(__name__, static_folder='dist', static_url_path='')
CORS(app)

# ... остальной код api_server.py ...
```

## Шаг 3: Установка PyInstaller

```cmd
cd python-backend
pip install pyinstaller
```

## Шаг 4: Создание spec-файла для PyInstaller

### 4.1 Создайте файл `whatsapp_sender.spec` в папке `python-backend`

```python
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('dist', 'dist'),  # Включить папку с фронтендом
        ('access_keys.json', '.'),  # Включить конфиг
    ],
    hiddenimports=[
        'flask',
        'flask_cors',
        'selenium',
        'webdriver_manager',
        'PIL',
        'base64',
        'json',
        'time',
        'datetime',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='WhatsAppSender',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,  # True = показывать консоль, False = скрыть
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='icon.ico'  # Опционально: добавьте иконку
)
```

## Шаг 5: Сборка EXE

### 5.1 Запустите PyInstaller

```cmd
cd python-backend
pyinstaller whatsapp_sender.spec
```

Процесс займет 2-5 минут. PyInstaller:
1. Проанализирует все зависимости
2. Соберет все необходимые файлы
3. Создаст исполняемый файл

### 5.2 Найдите готовый EXE

После успешной сборки EXE-файл будет находиться в:
```
python-backend/dist/WhatsAppSender.exe
```

## Шаг 6: Тестирование EXE

1. Скопируйте `WhatsAppSender.exe` в отдельную папку для тестирования
2. Дважды кликните по файлу
3. Должна открыться консоль, а затем браузер с приложением
4. Проверьте все функции: профили, отправку, массовую рассылку

## Шаг 7: Оптимизация и настройка

### Уменьшение размера EXE

```cmd
# Используйте UPX для сжатия (уже включено в spec-файле)
# Если размер слишком большой, можно исключить ненужные модули

# В spec-файле добавьте в excludes:
excludes=['tkinter', 'matplotlib', 'numpy', 'pandas'],
```

### Скрыть консоль (для финальной версии)

В `whatsapp_sender.spec` измените:
```python
console=False,  # Скрыть консольное окно
```

### Добавить иконку

1. Создайте или скачайте файл `icon.ico` (32x32 или 64x64 пикселей)
2. Поместите его в папку `python-backend`
3. В spec-файле уже указан `icon='icon.ico'`

## Распространение

### Минимальный пакет для пользователей:

```
WhatsAppSender/
├── WhatsAppSender.exe     # Основной файл
├── access_keys.json       # Конфигурация (опционально)
└── README.txt            # Инструкция для пользователей
```

### Создайте README.txt для пользователей:

```
WhatsApp Sender - Инструкция по запуску

1. Дважды кликните по WhatsAppSender.exe
2. Подождите несколько секунд - откроется браузер
3. Используйте приложение

Примечания:
- Требуется активное интернет-соединение
- Google Chrome будет загружен автоматически при первом запуске
- Все данные хранятся локально

Поддержка: [ваш email/контакт]
```

## Распространенные проблемы

### "Failed to execute script"
- Проверьте что все файлы в datas существуют
- Запустите с `console=True` чтобы увидеть ошибки
- Проверьте что все импорты в hiddenimports

### EXE файл слишком большой (>100MB)
- Исключите ненужные модули в `excludes`
- Убедитесь что UPX работает (`upx=True`)
- Рассмотрите использование `--onefile` вместо `--onedir`

### Антивирус блокирует EXE
- Это нормально для PyInstaller EXE
- Добавьте в исключения антивируса
- Подпишите EXE цифровой подписью (для коммерческого использования)

### "Cannot find Chrome binary"
- Selenium WebDriver Manager должен скачать Chrome автоматически
- Убедитесь что есть интернет при первом запуске
- Можно включить Chrome в сборку (увеличит размер)

### Профили не сохраняются
- Проверьте что `access_keys.json` включен в datas
- Убедитесь что у пользователя есть права на запись в папку

## Альтернативный метод: PyInstaller без spec-файла

Базовая команда (проще, но меньше контроля):

```cmd
pyinstaller --onefile ^
            --add-data "dist;dist" ^
            --add-data "access_keys.json;." ^
            --hidden-import flask ^
            --hidden-import selenium ^
            --name WhatsAppSender ^
            main.py
```

## Создание инсталлятора (опционально)

Для профессионального распространения используйте:

### Inno Setup (Windows):
1. Скачайте Inno Setup: https://jrsoftware.org/isdl.php
2. Создайте скрипт установки, который:
   - Копирует EXE
   - Создает ярлык на рабочем столе
   - Добавляет в меню Пуск
   - Настраивает удаление

### NSIS (Windows):
Альтернатива Inno Setup с большим контролем

## Итоговый чеклист

- [ ] Фронтенд собран (`npm run build`)
- [ ] Папка dist скопирована в python-backend
- [ ] Создан main.py
- [ ] Создан whatsapp_sender.spec
- [ ] PyInstaller установлен
- [ ] EXE успешно собран
- [ ] EXE протестирован на чистой системе
- [ ] Размер EXE оптимизирован
- [ ] Создана документация для пользователей
- [ ] (Опционально) Создан инсталлятор

## Дополнительные ресурсы

- PyInstaller документация: https://pyinstaller.org/
- Inno Setup: https://jrsoftware.org/isinfo.php
- UPX (компрессор): https://upx.github.io/

Удачи в создании вашего standalone приложения! 🚀
